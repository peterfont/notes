下面是当前网站完整的「统计 + 搜索引擎提交」流程。

## 一、网站信息

网站地址：

```
https://tuwen.panrp.top
```

核心页面：

```
https://tuwen.panrp.top/
https://tuwen.panrp.top/home
https://tuwen.panrp.top/templates
https://tuwen.panrp.top/xiaohongshu-cover
https://tuwen.panrp.top/xiaohongshu-long-image
https://tuwen.panrp.top/markdown-to-wechat
https://tuwen.panrp.top/wechat-editor
https://tuwen.panrp.top/douyin-image
```

SEO 文件：

```
https://tuwen.panrp.top/robots.txt
https://tuwen.panrp.top/sitemap.xml
```

## 二、51.la 网站统计

当前项目已经接入 51.la，文件位置：

```
/Users/panruipeng/worksapce/xhs-cover/next-app/app/components/analytics-51la.tsx
```

当前配置：

```
LA.init({
  id: "3QlZ17xCS1aUB0A6",
  ck: "3QlZ17xCS1aUB0A6",
  autoTrack: true,
  hashMode: true,
  screenRecord: true
})
```

功能含义：

- `autoTrack: true`：自动统计页面访问和基础行为
- `hashMode: true`：适配 React / Next.js 单页面路由变化
- `screenRecord: true`：开启录屏功能
- `id`、`ck`：对应 51.la 站点标识

查看数据：

1. 登录 [51.la](https://v6.51.la/)
2. 进入对应站点
3. 查看访问量、来源、页面、设备、地域、录屏等数据

当前接入方式：

```
<Script
  id="la-collect-sdk"
  src="https://sdk.51.la/js-sdk-pro.min.js"
  strategy="beforeInteractive"
  charSet="UTF-8"
/>
```

生产环境已经验证页面中存在 SDK 和 `LA.init` 配置。

### 后续建议增加业务事件

目前已经开启自动统计，但以下业务事件建议后续增加：

```
home_view
click_cover_tool
click_article_tool
use_template
click_generate
download_image
copy_wechat
share_link
generate_failed
```

事件统计最好封装成统一方法，例如：

```
export function trackEvent(name: string, data?: Record<string, unknown>) {
  if (typeof window === 'undefined') return;

  const la = (window as typeof window & {
    LA?: { track?: (name: string, data?: Record<string, unknown>) => void }
  }).LA;

  la?.track?.(name, data);
}
```

然后在按钮点击、生成成功、下载、复制等位置调用。

注意：录屏功能涉及用户隐私，正式运营时建议在隐私政策中说明，并根据实际用户地区增加必要的授权提示。

## 三、百度搜索资源平台

官方入口：[百度搜索资源平台](https://ziyuan.baidu.com/site/index)

百度官方支持文件验证、HTML 标签验证和 CNAME 验证。[官方验证说明](https://ziyuan.baidu.com/wiki/631)

### 1. 添加站点

添加：

```
https://tuwen.panrp.top
```

不要添加之前的：

```
https://xhs.panrp.top
```

那是另一个站点。

### 2. 文件验证

百度生成的验证文件：

```
baidu_verify_codeva-ItjKC5XdEZ.html
```

已部署到服务器根目录，访问地址：

[验证文件](https://tuwen.panrp.top/baidu_verify_codeva-ItjKC5XdEZ.html)

百度后台点击：

1. 确认验证文件可以正常访问
2. 点击“完成验证”
3. 验证成功后不要删除该文件

### 3. 提交 sitemap

验证成功后进入：

```
资源提交 → 链接提交 → Sitemap
```

提交：

```
https://tuwen.panrp.top/sitemap.xml
```

### 4. 百度普通收录 API

百度后台会生成当前站点专属接口：

```
http://data.zz.baidu.com/urls?site=https://tuwen.panrp.top&token=你的token
```

推送格式为每行一个 URL：

```
cat > urls.txt <<'EOF'
https://tuwen.panrp.top/
https://tuwen.panrp.top/home
https://tuwen.panrp.top/templates
https://tuwen.panrp.top/xiaohongshu-cover
https://tuwen.panrp.top/xiaohongshu-long-image
https://tuwen.panrp.top/markdown-to-wechat
https://tuwen.panrp.top/wechat-editor
https://tuwen.panrp.top/douyin-image
EOF
```

推送命令：

```
curl -H 'Content-Type:text/plain' \
  --data-binary @urls.txt \
  "http://data.zz.baidu.com/urls?site=https://tuwen.panrp.top&token=你的token"
```

成功反馈类似：

```
{
  "remain": 2,
  "success": 8
}
```

当前已经成功推送 8 个页面，百度返回：

```
{
  "success": 8,
  "remain": 2
}
```

`success` 代表百度接收成功，不代表一定立即收录。

注意：百度 API token 属于敏感凭证，不要写进前端代码、Git 仓库或公开文档。之前 token 已经出现在聊天内容中，建议后续在百度后台重新生成一个。

## 四、IndexNow 推送

IndexNow 主要覆盖 Bing、Yandex 等支持该协议的搜索引擎。

当前已经配置：

```
https://tuwen.panrp.top/17f84327001410abf1045099c599b91c.txt
```

并提交了 8 个页面，接口已经返回成功：

```
202 Accepted
```

IndexNow 验证文件不能删除，否则后续推送会失效。

## 五、Google 收录

Google 不支持使用百度 token 推送，需要使用 Search Console。

操作步骤：

1. 打开 [Google Search Console](https://search.google.com/search-console)
    
2. 添加资源：
    
    ```
    https://tuwen.panrp.top
    ```
    
3. 完成域名或网址前缀验证
    
4. 进入「站点地图」
    
5. 提交：
    
    ```
    sitemap.xml
    ```
    
6. 进入「网址检查」
    
7. 检查首页和核心工具页
    
8. 点击「请求编入索引」
    

建议优先检查：

```
https://tuwen.panrp.top/home
https://tuwen.panrp.top/xiaohongshu-cover
https://tuwen.panrp.top/xiaohongshu-long-image
https://tuwen.panrp.top/markdown-to-wechat
https://tuwen.panrp.top/douyin-image
```

## 六、Bing 收录

打开 [Bing Webmaster Tools](https://www.bing.com/webmasters)：

1. 添加 `https://tuwen.panrp.top`
    
2. 可以从 Google Search Console 导入
    
3. 提交 sitemap：
    
    ```
    https://tuwen.panrp.top/sitemap.xml
    ```
    

IndexNow 已经完成推送，因此 Bing 侧通常会更快发现页面。

## 七、上线后的检查方式

检查 robots：

```
curl https://tuwen.panrp.top/robots.txt
```

检查 sitemap：

```
curl https://tuwen.panrp.top/sitemap.xml
```

检查页面标题和 canonical：

```
curl -s https://tuwen.panrp.top/home | grep -E '<title>|canonical'
```

查看百度收录：

```
site:tuwen.panrp.top
```

查看 Google 收录：

```
site:tuwen.panrp.top
```

建议：

- 每周检查一次收录情况
- 新增落地页后提交 sitemap
- 新增重要页面时使用百度 API 和 IndexNow 推送
- 不要重复高频推送同一批旧链接
- 持续增加真实模板、案例和工具使用内容

现在网站已经完成基础统计、SEO 配置、百度推送和 IndexNow 推送，剩下主要是等待搜索引擎抓取，以及在 Google Search Console 和 Bing Webmaster Tools 中完成账号侧配置。